#include <stdio.h>

int main() {
    int pin;
    printf("[*] Enter the 4-digit Vault PIN: ");
    scanf("%d", &pin);

    // معادلة رياضية بسيطة لإنتاج قفزة شرطية واضحة في الاسمبلي
    int secret_check = (pin * 3) - 1337;

    if (secret_check == 8530) { 
        printf("[+] Vault Unlocked! Flag: pwn{CFG_m4st3r_jumps_unl0ck3d}\n");
    } else {
        printf("[-] Security Lockdown Activated!\n");
    }
    return 0;
}