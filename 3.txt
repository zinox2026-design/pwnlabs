#include <stdio.h>
#include <string.h>

int main() {
    char password[50];
    printf("[*] Enter the Master Password: ");
    scanf("%49s", password);

    // كلمة المرور مدمجة بشكل ثابت في الكود
    if (strcmp(password, "Sanaa_Cyber_2026") == 0) {
        printf("[+] Access Granted! Flag: pwn{st4t1c_str1ngs_4r3_34sy}\n");
    } else {
        printf("[-] Access Denied!\n");
    }
    return 0;
}